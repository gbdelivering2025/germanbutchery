# germanbutchery

WhatsApp-first e-commerce starter (Next.js frontend + NestJS backend).

Structure:
- frontend/ (Next.js)
- backend/ (NestJS)
- db/schema.sql (starter DB schema)

Quick start (local):
- Frontend: cd frontend && npm install && npm run dev
- Backend: cd backend && npm install && npm run start:dev

---
